<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <delete model="ir.ui.view"
            search="[('model', '=', 'school.finance.dashboard')]"/>
    <delete model="ir.actions.act_window"
            search="[('res_model', '=', 'school.finance.dashboard')]"/>
    <delete model="ir.model.access"
            search="[('name', 'in', ('school.finance.dashboard user', 'school.finance.dashboard.period user'))]"/>

    <record id="action_school_accounting_overview" model="ir.actions.client">
        <field name="name">School Financial Overview</field>
        <field name="tag">school_finance.accounting_overview</field>
    </record>
</odoo>
